<!DOCTYPE html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ProS - Gestão de Unidades</title>
    <link rel="icon" href="/images/favicon.png" type="image/png">

    <!-- CSS -->
    <link rel="stylesheet" href="/vendor/css/simplebar.css">
    <link rel="stylesheet" href="/vendor/css/sweetalert2.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com/">
    <link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,200..800;1,200..800&amp;display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="https://cdn.datatables.net/2.3.3/css/dataTables.dataTables.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/select/3.0.1/css/select.dataTables.css">
    <link rel="stylesheet" href="/vendor/css/tom-select.min.css">
</head>

<body
    class="font-theme flex flex-col h-full text-base font-normal bg-zinc-50 dark:bg-zinc-950 text-zinc-600 dark:text-zinc-300">

    <%- include('../partials/sidebar') %>

        <main class="flex-1 lg:ms-64 flex flex-col app-content">

            <%- include('../partials/header') %>

                <div class="px-4 lg:px-8 py-4">
                    <h2 class="text-xl mb-2">Gestão de Unidades</h2>
                    <ol class="flex gap-1 items-center text-sm">
                        <li class="flex items-center">
                            <a href="/dashboard"
                                class="text-zinc-400 hover:text-zinc-700 dark:hover:text-white">Dashboard</a>
                        </li>
                        <li class="flex items-center opacity-30 leading-none">
                            <span class="icon-[lucide--chevron-right] rtl:rotate-180"></span>
                        </li>
                        <li class="flex items-center"><span class="pointer-events-none">Unidades</span></li>
                    </ol>
                </div>

                <div class="content flex-grow lg:px-4">
                    <div class="grid grid-cols-1 px-4">
                        <div class="rounded-xl bg-white dark:bg-zinc-900 shadow-card mb-4">
                            <div
                                class="flex flex-wrap gap-2 items-center p-4 border-b border-zinc-500/10 dark:border-zinc-800">

                                <div class="flex-grow">
                                    <div class="relative">
                                        <input type="search" class="input !ps-9" name="unidadeSearch" id="unidadeSearch"
                                            placeholder="Buscar unidade...">
                                        <span
                                            class="iconify icon-[lucide--search] pointer-events-none absolute start-0 top-0 h-full flex items-center justify-center ms-3 text-muted"></span>
                                    </div>
                                </div>

                                <div class="flex items-center flex-wrap justify-end gap-2">
                                    <button id="inativarBtn" class="btn bg-amber-500 text-white hover:bg-amber-600"
                                        style="display: none;">
                                        Inativar Selecionada
                                    </button>

                                    <div class="flex-grow w-56 sm:w-auto">
                                        <select id="statusFilter" class="js-select w-full sm:w-56">
                                            <option value="" selected>Todos os Status</option>
                                            <option value="Ativo">Ativo</option>
                                            <option value="Inativo">Inativo</option>
                                        </select>
                                    </div>

                                    <div class="flex items-center gap-2">
                                        <a href="/unidade/novo"
                                            class="btn bg-primary shrink-0 text-white hover:bg-primary-deep">
                                            <span class="icon-[lucide--plus] text-lg me-1"></span>
                                            Nova Unidade
                                        </a>
                                    </div>
                                </div>
                            </div>

                            <table id="table_unidades" class="table-default">
                                <thead>
                                    <tr>
                                        <th></th>
                                        <th class="text-nowrap">Nome Fantasia</th>
                                        <th class="text-nowrap">Razão Social</th>
                                        <th class="text-nowrap">CNPJ</th>
                                        <th class="text-nowrap">Cidade / UF</th>
                                        <th class="text-nowrap">Status</th>
                                        <th class="text-nowrap" data-orderable="false">Ações</th>
                                    </tr>
                                </thead>
                                <!-- O CORPO DA TABELA ESTÁ VAZIO. O JAVASCRIPT VAI PREENCHER. -->
                                <tbody id="tableBody"></tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <%- include('../partials/footer') %>

        </main>

        <!-- ELEMENTO OCULTO: Guarda o JSON puro. -->
        <div id="dados-unidades-json" style="display: none;">
            <%= unidadesJson %>
        </div>

        <script src="/js/theme.js"></script>
        <script src="/vendor/js/jquery.min.js"></script>
        <script src="https://cdn.datatables.net/2.3.3/js/dataTables.js"></script>
        <script src="https://cdn.datatables.net/select/3.0.1/js/dataTables.select.js"></script>
        <script src="https://cdn.datatables.net/select/3.0.1/js/select.dataTables.js"></script>
        <script src="/vendor/js/sweetalert2.all.min.js"></script>

        <script>
            document.addEventListener('DOMContentLoaded', () => {
                // 1. LER OS DADOS DO ELEMENTO OCULTO
                let listaUnidades = [];
                try {
                    const divJson = document.getElementById('dados-unidades-json');
                    if (divJson) {
                        const rawJson = divJson.innerText;
                        if (rawJson) {
                            listaUnidades = JSON.parse(rawJson);
                        }
                    }
                } catch (e) {
                    console.error("Erro ao processar JSON:", e);
                }

                console.log("Unidades carregadas no JS:", listaUnidades);

                // 2. GERAR O HTML DA TABELA VIA JAVASCRIPT
                const tbody = document.getElementById('tableBody');

                if (listaUnidades.length === 0) {
                    tbody.innerHTML = '<tr><td colspan="7" class="text-center p-4">Nenhuma unidade encontrada.</td></tr>';
                } else {
                    listaUnidades.forEach((unidade, index) => {
                        const tr = document.createElement('tr');
                        tr.setAttribute('data-index', index);

                        const statusHtml = unidade.ativo
                            ? '<span class="badge bg-green-500/10 text-green-500 status-badge">Ativo</span>'
                            : '<span class="badge bg-zinc-500/10 text-zinc-500 status-badge">Inativo</span>';

                        tr.innerHTML = `
                    <td></td>
                    <td class="font-semibold text-zinc-900 whitespace-nowrap dark:text-white">${unidade.nome_fantasia || ''}</td>
                    <td class="whitespace-nowrap">${unidade.razao_social || '-'}</td>
                    <td class="whitespace-nowrap">${unidade.cnpj || ''}</td>
                    <td class="whitespace-nowrap">${unidade.cidade || ''} - ${unidade.estado || ''}</td>
                    <td>${statusHtml}</td>
                    <td>
                        <div class="flex items-center justify-end gap-1 relative z-[1]">
                            <a href="/unidade/editar/${unidade.id_unidade}" class="btn !p-1.5 hover:!text-primary">
                                <span class="icon-[lucide--edit] text-lg"></span>
                            </a>
                        </div>
                    </td>
                `;
                        tbody.appendChild(tr);
                    });
                }

                // 3. INICIALIZAR DATATABLE (CONFIGURAÇÃO IGUAL A TELA DE SERVIÇOS)
                const table = new DataTable('#table_unidades', {
                    lengthChange: false,
                    pageLength: 10,
                    scrollX: true,
                    dom: 'lrtip',
                    language: {
                        emptyTable: "Nenhuma unidade encontrada",
                        info: "Mostrando _START_ até _END_ de _TOTAL_ registros",
                        paginate: { first: "Primeiro", last: "Último", next: "Próximo", previous: "Anterior" },
                        select: { rows: "" }
                    },
                    columnDefs: [
                        {
                            orderable: false,
                            searchable: false,
                            render: DataTable.render.select(), // IGUAL SERVIÇOS
                            targets: 0,
                        }
                    ],
                    select: {
                        style: 'os', // IGUAL SERVIÇOS
                        selector: 'td:first-child',
                        headerCheckbox: false // IGUAL SERVIÇOS
                    },
                    order: [[1, 'asc']]
                });

                const searchInput = document.getElementById('unidadeSearch');
                searchInput.addEventListener('input', e => {
                    table.search(e.target.value).draw();
                });

                const inativarBtn = document.getElementById("inativarBtn");

                table.on('select deselect', function () {
                    const count = table.rows({ selected: true }).count();
                    inativarBtn.style.display = count > 0 ? "inline-flex" : "none";
                    inativarBtn.innerHTML = `<span ></span> Inativar (${count})`;
                });

                document.getElementById('statusFilter').addEventListener('change', function () {
                    table.columns(5).search(this.value).draw();
                });

                // 4. AÇÃO DE INATIVAR (COM VERIFICAÇÃO DE STATUS)
                inativarBtn.addEventListener('click', function () {
                    const selectedRows = table.rows({ selected: true }).nodes();
                    const selectedIds = [];
                    const unidadesJaInativas = [];

                    selectedRows.each(function (row) {
                        const index = row.getAttribute('data-index');

                        if (index !== null && listaUnidades[index]) {
                            const unidade = listaUnidades[index];

                            // VERIFICAÇÃO: Se já for 0 (Inativo), adiciona na lista de erro
                            if (unidade.ativo === 0) {
                                unidadesJaInativas.push(unidade.nome_fantasia);
                            } else {
                                // Se for 1 (Ativo), adiciona na lista para enviar
                                if (unidade && unidade.id_unidade) {
                                    selectedIds.push(unidade.id_unidade);
                                }
                            }
                        }
                    });

                    // SE TIVER ALGUÉM JÁ INATIVO, PARA TUDO E AVISA
                    if (unidadesJaInativas.length > 0) {
                        Swal.fire({
                            icon: 'warning',
                            title: 'Ação Bloqueada',
                            html: `A seguinte unidade <b>já está inativada</b>:<br><br><b>${unidadesJaInativas.join('<br>')}</b><br><br>Por favor, desmarque essas opções para continuar.`
                        });
                        return;
                    }

                    console.log("IDs capturados:", selectedIds);

                    if (selectedIds.length === 0) {
                        Swal.fire('Erro', 'Nenhuma unidade válida selecionada.', 'error');
                        return;
                    }

                    Swal.fire({
                        title: 'Tem certeza?',
                        text: `Você está prestes a inativar ${selectedIds.length} unidade(s).`,
                        icon: 'warning',
                        showCancelButton: true,
                        confirmButtonText: 'Sim, inativar',
                        cancelButtonText: 'Cancelar',
                        customClass: {
                            confirmButton: 'btn bg-amber-500 text-white hover:bg-amber-600 px-6 py-2 rounded-lg',
                            cancelButton: 'btn bg-zinc-200 text-zinc-800 hover:bg-zinc-300 px-6 py-2 rounded-lg ms-2'
                        },
                        buttonsStyling: false
                    }).then(async (result) => {
                        if (!result.isConfirmed) return;

                        try {
                            const response = await fetch('/unidade/inativar-multiplas', {
                                method: 'POST',
                                headers: { 'Content-Type': 'application/json' },
                                body: JSON.stringify({ ids: selectedIds })
                            });

                            const data = await response.json();

                            if (response.ok && data.success) {
                                Swal.fire({
                                    icon: 'success',
                                    title: 'Sucesso!',
                                    text: data.message,
                                    confirmButtonText: 'OK',
                                    customClass: { confirmButton: 'btn bg-primary text-white px-6 py-2 rounded-lg' },
                                    buttonsStyling: false
                                }).then(() => {
                                    window.location.reload();
                                });
                            } else {
                                Swal.fire({
                                    icon: 'error',
                                    title: 'Atenção',
                                    text: data.message || "Erro desconhecido"
                                });
                            }
                        } catch (error) {
                            console.error(error);
                            Swal.fire({
                                icon: 'error',
                                title: 'Erro!',
                                text: 'Não foi possível conectar ao servidor.'
                            });
                        }
                    });
                });
            });
        </script>

        <script src="/vendor/js/tom-select.complete.min.js"></script>
        <script>
            document.querySelectorAll('.js-select').forEach(select => {
                new TomSelect(select, { create: false, allowEmptyOption: true });
            });
        </script>
</body>

</html>